<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    android:gravity="top">

    <!-- IMAGE -->
    <ImageView
        android:id="@+id/cartItemImage"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:contentDescription="@string/cart_product_image_cd"
        android:scaleType="centerCrop"
        android:src="@drawable/logo" />

    <!-- TEXTS -->
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:paddingStart="8dp"
        android:gravity="top">

        <TextView
            android:id="@+id/cartItemName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/dark_bg"
            android:textSize="14sp"
            android:textStyle="bold"
            android:maxLines="2"
            android:ellipsize="end"
            tools:text="Sneaker name" />

        <TextView
            android:id="@+id/cartItemSize"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_grey"
            android:textSize="12sp"
            android:layout_marginTop="3dp"
            tools:text="Size: 42" />

        <TextView
            android:id="@+id/cartItemPrice"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/dark_bg"
            android:textSize="13sp"
            android:textStyle="bold"
            android:layout_marginTop="3dp"
            tools:text="38 200 ₽" />

    </LinearLayout>

    <!-- QUANTITY CONTROLS -->
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="top|center_vertical">

        <ImageView
            android:id="@+id/cartMinus"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:contentDescription="@string/cart_decrease_quantity_cd"
            android:padding="5dp"
            android:src="@drawable/ic_minus" />

        <TextView
            android:id="@+id/cartQty"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:gravity="center"
            android:textColor="@color/dark_bg"
            android:textSize="14sp"
            tools:text="1" />

        <ImageView
            android:id="@+id/cartPlus"
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:contentDescription="@string/cart_increase_quantity_cd"
            android:padding="5dp"
            android:src="@drawable/ic_plus" />

    </LinearLayout>

    <!-- REMOVE BUTTON -->
    <ImageView
        android:id="@+id/cartRemove"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:layout_marginStart="4dp"
        android:contentDescription="@string/cart_remove_item_cd"
        android:padding="5dp"
        android:src="@drawable/ic_delete" />

</LinearLayout>